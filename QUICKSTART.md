# 快速开始 - 多用户版本

## 🚀 快速部署

### 方式一：Docker Compose（推荐）

```bash
# 1. 启动服务
docker-compose up -d

# 2. 查看日志确认启动成功
docker-compose logs web

# 3. 访问管理界面
http://localhost:8088
```

**默认登录账号**：
- 用户名：`admin`
- 密码：`admin123456`

### 方式二：本地部署

```bash
# 1. 编译后端
go build -o ecloud-server .

# 2. 启动后端
./ecloud-server server

# 3. 访问管理界面
http://localhost:8088
```

**默认登录账号**：
- 用户名：`admin`
- 密码：`admin123456`

---

## 👤 默认账号

默认管理员账号在 `.env` 文件中配置：

- **用户名**：`admin`
- **密码**：`admin123456`

⚠️ **生产环境请务必修改为强密码！**

### 修改默认账号密码

编辑 `.env` 文件（首次启动前）：

```bash
ADMIN_USERNAME=your_username
ADMIN_PASSWORD=your_strong_password
```

**注意**：
- 仅在**首次启动前**修改有效
- 首次启动后，修改 `.env` 不会影响已创建的管理员账号
- 如需修改密码，请登录后台通过"修改密码"功能

---

## 📋 主要功能

### 管理员功能
- ✅ 管理所有用户（创建、编辑、删除）
- ✅ 查看所有云账号
- ✅ 查看所有操作日志
- ✅ 重置用户密码
- ✅ 修改自己的密码

### 普通用户功能
- ✅ 管理自己的云账号
- ✅ 查看自己的操作日志
- ✅ 修改自己的密码

---

## 🔧 常见操作

### 1. 创建新用户（管理员）

1. 登录管理员账号
2. 点击侧边栏"用户管理"
3. 点击"添加用户"按钮
4. 填写用户信息：
   - 用户名（3-32位字母数字下划线）
   - 密码（至少8位）
   - 角色（管理员/普通用户）
   - 显示名称（选填）
   - 邮箱（选填）
5. 点击"确定"

### 2. 修改密码

1. 点击右上角用户名下拉菜单
2. 选择"修改密码"
3. 输入旧密码和新密码
4. 点击"确定"（会自动退出，需要重新登录）

### 3. 添加云账号

1. 点击侧边栏"账号管理"
2. 点击"添加账号"按钮
3. 选择账号类型（公众版/政企版）
4. 填写账号信息
5. 配置监控参数
6. 点击"确定"

---

## 🔐 权限说明

| 功能 | 管理员 | 普通用户 |
|------|--------|----------|
| 用户管理 | ✅ | ❌ |
| 查看所有云账号 | ✅ | ❌ |
| 管理自己的云账号 | ✅ | ✅ |
| 查看所有日志 | ✅ | ❌ |
| 查看自己的日志 | ✅ | ✅ |
| 修改自己的密码 | ✅ | ✅ |
| 重置他人密码 | ✅ | ❌ |

---

## 🔒 安全建议

1. **生产环境在 `.env` 文件中设置强密码**（至少12位，包含大小写字母、数字、特殊字符）
2. **不要删除最后一个管理员账号**
3. **定期备份数据库文件**（`ecloud.db`）
4. **不要将 `.env` 文件提交到版本控制系统**
5. **建议配置 HTTPS**

---

## 🐛 故障排查

### 问题：忘记管理员密码

**解决方案**：
```bash
# 1. 停止服务
docker-compose down

# 2. 删除数据库文件
rm ecloud.db

# 3. 重新启动（会重新创建管理员账号）
docker-compose up -d
```

### 问题：登录后显示"权限不足"

**原因**：普通用户尝试访问管理员功能

**解决方案**：联系管理员升级为管理员角色

### 问题：无法删除用户

**可能原因**：
1. 尝试删除最后一个管理员账号（系统不允许）
2. 尝试删除自己（系统不允许）

---

## 📊 端口说明

- **8088**：Web 管理界面 + API 接口

---

## 📁 数据存储

- **数据库文件**：`ecloud.db`（SQLite）
- **位置**：项目根目录（或 `DATA_DIR` 环境变量指定的目录）

**备份建议**：定期复制 `ecloud.db` 文件

---

## 🔄 从旧版本升级

旧版本数据会自动迁移：

1. **auth 表保留**（兼容旧版 JWT 密钥）
2. **首次启动自动创建管理员账号**
3. **旧账号数据完整保留**（会自动关联到管理员）

---

## 📞 获取帮助

- 查看完整文档：`MULTI_USER_MIGRATION.md`
- 查看 API 文档：`http://localhost:8088/api/`

---

**最后更新**：2026-06-16
